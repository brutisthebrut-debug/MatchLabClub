# Memory index

- [Stripe webhook via stripe-replit-sync](stripe-replit-sync.md) — package types differ from skill template; webhook before express.json; read-only against stripe schema; root-resolved deps.
- [vitest broken/missing in nldc + nldc-mobile](nldc-mobile-typecheck.md) — "Cannot find module vitest" red workflows: if vitest pkg dir is gone (post-merge partial install) run `pnpm install`; if only test/config tsc errors remain it's a skip-able quirk.
